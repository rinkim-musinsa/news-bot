name: Keepalive
on:
  schedule:
    - cron: '0 0 1 * *'  # 매월 1일 자동 실행 (60일 제한 안전하게 커버)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: gautamkrishnar/keepalive-workflow@v2
